# Wrap up: weekly consolidation, README fix, GC validation

## 1. Weekly consolidation schedule
Add to `consolidate.yml`: `schedule: cron "0 8 * * 0"` (Sundays 08:00 UTC, after the weekend GC slot), keep dispatch inputs as-is.

## 2. Memory-bound streaming consolidation (precondition for the schedule)
Rework `consolidate_partition` to stream: read each source file via `iter_batches` into a `ParquetWriter` (memory peaks at one batch, never the partition), keep the id-set dedupe, and add `max_bytes`/`max_rows` caps to `plan()` so oversized partitions are reported and skipped. Tests updated; serial-per-tile stays as is.

## 3. README nit
Add `import coiled` to the sample; otherwise leave as is (verified valid).

## 4. Account for the −2 rows
Trace the table's snapshot summaries (metadata-only) to identify the commit and partition where 68,067,891 → 68,067,889, and confirm it was the built-in dedupe. Report findings.

## 5. GC deletion validation (the remaining task from your ask)
Against the scratch `warehouse-ci` only:
- dry-run GC with a crafted inventory missing a few keys + head_fn simulating their deletion → expect candidates == confirmed, zero writes;
- live run → affected partition files rewritten, index rows soft-deleted, table rebuilt;
- verify: deleted ids gone from search, counts reconciled. Production GC stays untouched — it runs for real only after you review this validation.

## 6. Ship
Gates (ruff/mypy/tests), push branch, open PR for consolidation + GC validation + schedule.

Out of scope: GC-via-Iceberg-metadata tightening (follow-up), production GC live run.